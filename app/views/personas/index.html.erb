<div class="row">
  <div class="col-md-12">
    <h1><%= current_user.nombre %></h1>
    <h2><%= current_user.email %></h2>
    <h3><%= current_user.sexo %></h3>
    <table class="table">
      <% @personas.each do |persona| %>
        <tr>
          <td>
            <%= link_to persona.cedula, persona_path(persona) %>
          </td>
          <td><%= persona.nombre %></td>
          <td><%= persona.email %></td>
          <td>
            <%= link_to "Editar", edit_persona_path(persona) %>
          </td>
          <td>
            <%= link_to persona_path(persona), method: :delete, data: {confirm: "¿Está seguro?"}, class: "btn btn-default"  do %>
              Eliminar
              <i class="fa fa-user" aria-hidden="true"></i>
            <% end %>
            <br>
            <%= link_to persona_path(persona), method: :delete, data: {confirm: "¿Está seguro?"}, class: "btn btn-danger"  do %>
              <%= fa_icon "bicycle" %>
              Eliminar
            <% end %>


          </td>
        </tr>
      <% end %>
    </table>
  </div>
</div>
